# Hazlitt *Table-Talk* — targeted structure audit

No corpus data was changed. This is a source-to-fixture diagnostic.

## Observed sequence

| Stage | Successive structure |
| --- | --- |
| Gutenberg source, lines 434–447 | `<p>` ending “shadowy abstraction,” → `<pre>` containing “Pure in the last recesses of the mind,” → a new `<p>` beginning “and derive neither…” |
| Normalized V2 | `paragraph` → `verse` → `paragraph`, with the same text. |
| Bookfin page 4 | The same three blocks, unsplit. |
| Mobile fixture | Exact copy of page 4 made by the sync script. |

The first paragraph begins “I have more satisfaction…” because it is a real
source `<p>`, not an isolated fragment generated by pagination.

## Cause and decision

The vertical space is expected from the source’s explicit `<pre>` and the
normalizer’s documented `<pre> → verse` rule. Pagination did not create or
split the block; the mobile fixture does not alter it. Merging this sequence
would convert an intentionally preformatted quoted line into prose and is not
safe.

## Related signature

`paragraph → verse → paragraph` occurs 117 times in Table-Talk and once in
`fr-stendhal-chroniques-italiennes`. These are candidates for editorial review,
not evidence of a generic pagination defect. A future schema may distinguish a
quoted verse line from a poetic passage, but that is outside this renderer audit.
